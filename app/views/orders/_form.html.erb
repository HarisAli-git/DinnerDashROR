<%= form_for order, :url => {:controller => 'orders', :action => 'update', order_id: order.id, status: order.status}, method: :patch do |form| %>   
    <%=form.submit 'Change Status', :class => 'btn btn-outline-warning' %>
<% end %>